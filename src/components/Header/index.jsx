import Logo from '../../assets/logo-store.jpg';
import styles from './style.module.css';
export const Header = () => {
    
    return (
        <header className=
        {styles.flexbox}>
          <img src={Logo} alt="logo" />
        </header>
        );
}